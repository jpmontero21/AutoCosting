@page
@model PersonalDataModel
@{
    ViewData["Title"] = "Información Personal";
    ViewData["ActivePage"] = ManageNavPages.PersonalData;
}

<h4>@ViewData["Title"]</h4>

<div class="row">
    <div class="col-md-6">
        <p>Su cuenta contiene datos personales que nos ha proporcionado. Esta página le permite descargar o borrar esos datos.</p>
        <p>
            <strong>Eliminar estos datos eliminará de forma permanente su cuenta, y esto no se puede recuperar.</strong>
        </p>
        <form asp-page="DownloadPersonalData" method="post" class="form-group">
            <button class="btn btn-primary" type="submit">Descargar</button>
        </form>
        <p>
            <a asp-page="DeletePersonalData" class="btn btn-danger">Eliminar</a>
        </p>
    </div>
</div>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}